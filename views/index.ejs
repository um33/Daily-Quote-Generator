<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Daily Quote</title>
  <link rel="stylesheet" href="style.css">
</head>

<body>
  <div class="container">
    <h1>Daily Quote</h1>
    <% if (locals.quote && locals.author) { %>
      <blockquote>
        <p><%= quote %></p>
        <footer>- <%= author %></footer>
      </blockquote>
    <% } else { %>
      <p><%= error %></p>
    <% } %>
  </div>
</body>

</html>
